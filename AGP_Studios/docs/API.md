# RaStudios ↔ RaOS API Reference

## Authentication
- Token-based via `/api/auth`
- Example request/response

## WebSocket Events
- `game.start`
- `asset.upload`
- `chat.message`
- How to add new event handlers

## REST Endpoints
- `/api/games/` — list games
- `/api/assets/` — upload asset
- Example payloads

## Adding New Endpoints
- How to register in `api/` folder
